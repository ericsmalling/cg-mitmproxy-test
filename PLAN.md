A customer wants to use a zscaler mitm proxy to funnel all pypi, npm and maven artifact pulls to use their chainguard library repo at cgr.dev.

I don't have a zscaler environment to experiment with so I'd like to use a local docker or kubernetes (kind) network and a similar proxy.  Perhaps using the mitmproxy ( https://www.mitmproxy.org/ ) tool.   There is a chainguard mitmproxy apk available that we can use if it helps.

